# =====================================================
# PRODUCTION ENVIRONMENT VARIABLES
# =====================================================
# Copy this file to .env.local or set these in your hosting platform
# 
# SECURITY NOTICE: All API keys should be:
# - At least 32 characters long
# - Randomly generated (use: openssl rand -hex 32)
# - Kept secret and never committed to version control
# =====================================================

# =====================================================
# REQUIRED: Authentication Keys
# =====================================================

# Admin API Key - Used for admin endpoints (/api/admin/*)
# Generate with: openssl rand -hex 32
ADMIN_API_KEY=your_admin_key_here_at_least_32_chars

# Ingest API Key - Used for signal ingestion (/api/signals/ingest)
# Generate with: openssl rand -hex 32
INGEST_API_KEY=your_ingest_key_here_at_least_32_chars

# =====================================================
# REQUIRED: Database Configuration
# =====================================================

# Azure Cosmos DB Connection String
# Format: mongodb://username:password@host:port/?ssl=true&...
AZURE_COSMOS_CONNECTION_STRING=your_cosmos_connection_string

# Azure Cosmos DB Database Name
AZURE_COSMOS_DB_NAME=freyatrades

# =====================================================
# REQUIRED: IP Geolocation Service
# =====================================================

# IP2Location API Key(s) - Comma-separated for fallback
# Get keys from: https://www.ip2location.io/
IP2LOCATION_API_KEY=your_primary_key,your_backup_key

# =====================================================
# SECURITY SETTINGS
# =====================================================

# Enable rate limiting (recommended: true in production)
RATE_LIMIT_ENABLED=true

# Enable debug endpoints (recommended: false in production)
# Only set to 'true' for troubleshooting
DEBUG_ENDPOINTS_ENABLED=false

# CORS Allowed Origins (comma-separated)
# Leave empty to allow only same-origin requests
# Example: https://yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=https://yourdomain.com

# =====================================================
# ACCESS CONTROL SETTINGS
# =====================================================

# Countries to restrict access from (comma-separated ISO codes)
RESTRICTED_COUNTRIES=IN

# Preview duration in seconds (3 minutes = 180 seconds)
PREVIEW_DURATION_SECONDS=180

# Time consumed threshold before blocking (seconds)
TIME_CONSUMED_THRESHOLD=60

# VPN detection settings
VPN_MAX_RETRIES=5
VPN_RETRY_WINDOW_HOURS=2

# =====================================================
# EXTERNAL LINKS
# =====================================================

NEXT_PUBLIC_TRIAL_TELEGRAM_URL=https://t.me/your_channel
NEXT_PUBLIC_TRIAL_WHOP_URL=https://whop.com/your-product
NEXT_PUBLIC_INNER_CIRCLE_URL=https://your-inner-circle-link

# =====================================================
# DEPLOYMENT
# =====================================================

# Node environment (production/development)
NODE_ENV=production

# =====================================================
# SECURITY CHECKLIST
# =====================================================
# Before deploying, verify:
# [ ] ADMIN_API_KEY is set and at least 32 characters
# [ ] INGEST_API_KEY is set and at least 32 characters
# [ ] RATE_LIMIT_ENABLED is set to true
# [ ] DEBUG_ENDPOINTS_ENABLED is set to false (or not set)
# [ ] ALLOWED_ORIGINS is configured for your domain
# [ ] All keys are unique and randomly generated
# [ ] This file is NOT committed to version control
# =====================================================

